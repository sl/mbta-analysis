const configurations = {
  development: {
    backendAPI: 'http://localhost:5000/api'
  },
  development: {
    backendAPI: 'http://localhost:5000/api'
  },
};

const config = process.env.node_env || 'development';